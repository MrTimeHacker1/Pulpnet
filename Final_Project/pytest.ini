[pytest]
# Keep test output readable; the parser/retrieval tests touch real PDFs + the
# live Qdrant index, so they are slower by design.
addopts = -ra
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
